#! /bin/bash

# This file is a simple wrapper around horizon that establishes the proper
# environment variables before executing the actual binary.

source /opt/viichain/horizon/etc/vii-horizon.env
exec /usr/bin/vii-horizon $@
